@model IEnumerable<SotDLCharGen.Models.ClockworkPurpose>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClockworkPurposeValue)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClockworkPurposeValue)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClockworkPurposeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClockworkPurposeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClockworkPurposeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
